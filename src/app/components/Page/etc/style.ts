import styled from "@emotion/styled";
import { MAX_WIDTH, Spacings } from "../../../../theme";

export const Page = styled.div`
  padding: ${Spacings.medium} ${Spacings.large};
  max-width: ${MAX_WIDTH};
  margin: 0 auto;
`;
